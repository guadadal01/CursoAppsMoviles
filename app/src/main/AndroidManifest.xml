<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/descarga"
        android:label="@string/PrimeraApp"
        android:roundIcon="@drawable/descarga"
        android:supportsRtl="true"
        android:theme="@style/Theme.CursoAppsMoviles"
        tools:targetApi="31">
        <activity
            android:name=".RGB.RGBColores"
            android:exported="false"
            android:label="@string/title_activity_rgbcolores"
            android:theme="@style/Theme.CursoAppsMoviles" />
        <activity
            android:name=".halloween.PeliculasHalloween"
            android:exported="true"
            android:label="@string/title_activity_peliculas_halloween"
            android:theme="@style/Theme.CursoAppsMoviles">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/PrimeraApp"
            android:theme="@style/Theme.CursoAppsMoviles">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>